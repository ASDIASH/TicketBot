const Discord = require("discord.js");
const config = require("../data/configurações.json");

module.exports.run = async (client, message, args) => {
  // if (message.channel.type === "dm") {
  //   message.channel.send("Este comando deve ser usado em um servidor");
  //   return;
  // } //Não responde DM

  // if (!message.member.hasPermission('//a perm q vc quer')) {
  //   return message.channel.send('Você não tem permissão para executar esse comando.')
  // }

  // if (message.author.id == config.dono) {
  //   return
  // } // só responde o dono do bot

  var string = args.join(" "); //converte os args para string


}; 

//releva

