const Discord = require('discord.js')
const db = require('quick.db')

module.exports.run = (client, message) => {
    console.log(`Bot iniciado com sucesso`);
}
// isso e a atividade do BOT so trocar "PLAYING" por algum desses anteriores